name : First Github Workflow

on:
  pull_request:
    branches: [ main ]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps: 
    - name: first step
      run: echo This is the first step of my first job.